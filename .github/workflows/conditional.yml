name: conditional test

on:
  workflow_dispatch:


jobs:
  job:
    runs-on: ubuntu-latest
    steps: 
      - run: echo 'foo={"bar":false}' >> "$GITHUB_OUTPUT"
        id: test
      - if: ${{ !fromJSON(steps.test.outputs.foo).bar }}
        run: echo "print"